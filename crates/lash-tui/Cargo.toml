[package]
name = "lash-tui"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Terminal UI for Lash"

[dependencies]
lash-types = { workspace = true }
lash-core = { workspace = true }
lash-db = { workspace = true }
ratatui = { workspace = true }
crossterm = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
rusqlite = { workspace = true }
once_cell = { workspace = true }
regex = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
insta = { workspace = true }
# Backdating file mtimes in tests that need to prove "old file" behaviour
# (the activity-bar backfill must not depend on file freshness). filetime
# is already pulled in transitively by notify so no new transitive cost.
filetime = "0.2"
